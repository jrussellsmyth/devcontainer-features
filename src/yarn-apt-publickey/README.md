# Yarn APT public key updater

A feature to update the Yarn APT public key. A workaround for the expired Yarn public key borking most debian based devcontainer images.

## Example Usage

```json
"features": {
    "ghcr.io/jrussellsmyth/devcontainer_features/yarn-apt-publickey": {}
}
```